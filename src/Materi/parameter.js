let mintaData = (param1, param2, string, number, undefined) => {
   console.log(param1, param2, string, number, undefined);
}

mintaData(
   "ini Parameter1",
   "ini Parameter 2",
   "ini Parameter String",
   4
);

// parameter mirip seperti variable kosong/undefined yang mana nanti dapat dikirim dan seperti kumpullan array